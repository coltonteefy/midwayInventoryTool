name: Test Secret

on:
  push:
    branches: [ "main" ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Show API_KEY
        run: echo "API_KEY is $API_KEY"
        env:
          API_KEY: ${{ secrets.API_KEY }}

